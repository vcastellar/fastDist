% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark_parallelDist.R
\name{crossdist_parallelDist}
\alias{crossdist_parallelDist}
\title{Compute Cross Distances with `parallelDist` in Blocks}
\usage{
crossdist_parallelDist(
  A,
  B,
  method = "euclidean",
  p = NULL,
  threads = NULL,
  block_size = NULL
)
}
\arguments{
\item{A}{Numeric matrix (or matrix-like object) of source observations.}

\item{B}{Numeric matrix (or matrix-like object) of target observations.}

\item{method}{Distance method. Supported values are `"euclidean"`,
`"manhattan"`, and `"minkowski"`.}

\item{p}{Minkowski exponent. Required when `method = "minkowski"`.}

\item{threads}{Optional number of threads passed to
`parallelDist::parDist()`.}

\item{block_size}{Number of rows of `B` processed per iteration. If `NULL`,
defaults to `nrow(A)`.}
}
\value{
A numeric matrix of size `nrow(A) x nrow(B)` with cross distances.
}
\description{
Computes pairwise distances between rows of `A` and rows of `B` using
`parallelDist::parDist()`, processing `B` by blocks to reduce memory
pressure.
}
