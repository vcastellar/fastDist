% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark_parallelDist.R
\name{benchmark_parallelDist}
\alias{benchmark_parallelDist}
\title{Benchmark `fastDist` vs `parallelDist`}
\usage{
benchmark_parallelDist(
  A = NULL,
  B = NULL,
  a_nrow = 1000L,
  n_features = 1000L,
  b_sizes = c(1000L, 5000L, 10000L, 20000L),
  methods = c("euclidean", "manhattan", "minkowski"),
  minkowski_p = 3,
  times = 1L,
  seed = 123,
  unit = "s",
  threads = NULL,
  block_size = NULL,
  check = TRUE
)
}
\arguments{
\item{A}{Optional numeric matrix of source observations. If `NULL`, a random
matrix is generated.}

\item{B}{Optional numeric matrix of target observations. If `NULL`, a random
matrix is generated.}

\item{a_nrow}{Number of rows to generate for `A` when `A` is `NULL`.}

\item{n_features}{Number of columns/features for generated matrices.}

\item{b_sizes}{Integer vector with the evaluated numbers of rows for `B`.}

\item{methods}{Character vector of methods to benchmark. Supported values are
`"euclidean"`, `"manhattan"`, and `"minkowski"`.}

\item{minkowski_p}{Exponent used when benchmarking the Minkowski distance.}

\item{times}{Number of repetitions passed to `microbenchmark`.}

\item{seed}{Random seed used when generating `A` and `B`.}

\item{unit}{Time unit for benchmark results (passed to `microbenchmark`).}

\item{threads}{Optional number of threads for `parallelDist`.}

\item{block_size}{Optional block size used by [`crossdist_parallelDist()`].}

\item{check}{Logical; if `TRUE`, validate that both implementations produce
equivalent results before timing.}
}
\value{
A list with benchmark parameters, a summary table, raw benchmark
  objects, and the matrices used in the benchmark run.
}
\description{
Runs benchmarking experiments comparing [`fdist()`] and
[`crossdist_parallelDist()`] across multiple methods and `B` matrix sizes.
Optional correctness checks can verify numerical agreement before timing.
}
