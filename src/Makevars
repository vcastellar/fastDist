# Enable OpenMP where the toolchain supports it; R supplies empty flags
# otherwise, so the package still builds (single-threaded) without it.
PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS)
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS) $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
